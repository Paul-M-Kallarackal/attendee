# AWS Configuration for local development
# You need to set these to your actual AWS credentials
AWS_DEFAULT_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_SESSION_TOKEN=your_session_token_here_if_needed
AWS_RECORDING_STORAGE_BUCKET_NAME=static.cdn.sparrowcrmstaging.com

# Database Configuration (if different from defaults)
# POSTGRES_HOST=localhost

# Redis Configuration (if different from defaults)
# REDIS_URL=redis://localhost:6379/5
